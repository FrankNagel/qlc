<?xml version="1.0" encoding="UTF-8"?><graph xmlns="http://www.xces.org/ns/GrAF/1.0/" xmlns:graf="http://www.xces.org/ns/GrAF/1.0/">
    <graphHeader>
        <labelsDecl>
        </labelsDecl>
    </graphHeader>

<% offset = 0 ; i = 0 %>
% for e in c.entries:
	% for a in e.annotations_sorted_by_type_and_start(c.annotationtypes):
		<region xml:id="seg-a-${c.annotationname}-${i}" anchors="${offset + a.start} ${offset + a.end}"/> <% i += 1 %>
	% endfor
	<% offset += len(e.fullentry) + 1 %>
% endfor

</graph>