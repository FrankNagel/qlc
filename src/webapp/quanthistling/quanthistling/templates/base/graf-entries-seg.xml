<?xml version="1.0" encoding="UTF-8"?><graph xmlns="http://www.xces.org/ns/GrAF/1.0/" xmlns:graf="http://www.xces.org/ns/GrAF/1.0/">
    <graphHeader>
        <labelsDecl>
        </labelsDecl>
    </graphHeader>

<% offset = 0 %>
% for i, e in enumerate(c.entries):
	<region xml:id="seg-e-${i}" anchors="${offset} ${offset + len(e.fullentry)}"/> <% offset = offset + len(e.fullentry) + 1 %>
% endfor

</graph>