<%inherit file="/base/graf.xml"/>
<%namespace name="component" file="/component/entry.xml" />

<chunkList>
    % for e in c.entries:
        ${component.annotations(e)}
    % endfor
</chunkList>